name: PAXG Full Pipeline (Download, Split & Encrypt)

on:
  workflow_dispatch:
    inputs:
      force_rebuild:
        description: 'بازسازی اجباری (yes/no)'
        required: false
        default: 'no'

permissions:
  contents: write

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 180   # برای دانلود و پردازش داده‌های زیاد زمان بیشتری لازم است
    steps:
      - name: 1. دریافت کد مخزن
        uses: actions/checkout@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: 2. راه‌اندازی پایتون
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 3. نصب کتابخانه‌ها
        run: |
          pip install --upgrade pip
          pip install pandas requests cryptography

      - name: 4. اجرای اسکریپت اصلی
        env:
          DATA_PASSWORD: ${{ secrets.DATA_PASSWORD }}
        run: |
          echo "🚀 شروع اجرای paxg_pipeline.py"
          python encrypted_data/paxg_pipeline.py
          echo "✅ اسکریپت با موفقیت تمام شد"

      - name: 5. ذخیره تغییرات در مخزن (commit & push)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "actions@github.com"
          git add data/All_Coins_Combined/ data/PAXGUSDT/ encrypted_data/data.enc zips/ 2>/dev/null || true
          git diff --staged --quiet || {
            git commit -m "PAXG pipeline: update data, split and encrypt"
            git pull --rebase --autostash origin main
            git push origin main
            echo "✅ تغییرات با موفقیت ارسال شد."
          }

      - name: 6. گزارش نهایی
        run: |
          echo "======================================="
          echo "✅ کار آماده‌سازی داده‌های PAXG با موفقیت انجام شد"
          echo "📂 تعداد CSVهای ساخته شده:"
          ls -la data/All_Coins_Combined/ | wc -l
          echo "📦 فایل رمزنگاری: encrypted_data/data.enc"
          ls -lh encrypted_data/data.enc
          echo "======================================="
